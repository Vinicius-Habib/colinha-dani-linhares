{
  "name": "colinha-dani-linhares-v2",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "sync": "node scripts/sync-tse.js",
    "check": "node --check server.js && node --check public/app.js && node --check scripts/sync-tse.js",
    "test": "node --test"
  },
  "dependencies": {
    "adm-zip": "^0.5.16",
    "express": "^5.1.0",
    "iconv-lite": "^0.6.3"
  },
  "engines": { "node": ">=20" }
}